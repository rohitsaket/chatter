{
  "name": "@chatter/database",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "db:migrate": "node --env-file-if-exists=../../.env node_modules/prisma/build/index.js migrate deploy",
    "db:migrate:dev": "node --env-file-if-exists=../../.env node_modules/prisma/build/index.js migrate dev",
    "db:seed": "node --env-file-if-exists=../../.env dist/seed.js",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@chatter/config": "workspace:*",
    "@prisma/client": "^6.19.1",
    "argon2": "^0.44.0"
  },
  "devDependencies": {
    "@chatter/eslint-config": "workspace:*",
    "@chatter/typescript-config": "workspace:*",
    "prisma": "^6.19.1",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
