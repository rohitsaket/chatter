{
  "name": "@chatter/test-utils",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@chatter/database": "workspace:*"
  },
  "devDependencies": {
    "@chatter/eslint-config": "workspace:*",
    "@chatter/typescript-config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
