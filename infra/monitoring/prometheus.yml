# Prometheus scrape config for Chatter.
# The API exposes text-format metrics at /metrics (http_requests_total,
# http_request_duration_ms_sum/count) plus /health for liveness.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: chatter-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:4000"]
